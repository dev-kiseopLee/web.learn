extends ../layout

block content
  main.container
    a(class='d-block my-3' href=`/blog/new`) New Post 
    div(class='card')
      div(class='card-body p-4')
        div.row
          div(class='col m-2')
            - for(let blog of blogs)
                a(class='card-title fw-bold fs-4' href=`blog/show/${blog.id}`)= blog.title
                p(class='card-text fs-5 fw-semibold mt-3 gray-600')= blog.description
                div(class='d-flex flex-stack flex-wrap mb-2')
                  span(class='tag badge fw-bold my-2') HTML
          
